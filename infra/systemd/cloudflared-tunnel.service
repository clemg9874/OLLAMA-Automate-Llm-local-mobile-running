[Unit]
Description=Cloudflared tunnel for local API
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/cloudflared tunnel --url http://localhost:3000
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
